package constants

const (
	// DefaultDb is the default sqlite database file
	DefaultDb = "mydb.db"
	// DefaultIDColumn is the default name of the primary key in a SQL table
	DefaultIDColumn = "id"
	// DataSourceEnvVar is the environment variable for the path to datasource files
	DataSourceEnvVar = "MYMINT_SOURCE_DIR"
)
